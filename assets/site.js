const canvas=document.getElementById('hebrewRain');
if(canvas){const ctx=canvas.getContext('2d');const letters='אבגדהוזחטיכלמנסעפצקרשתםןףךץיהוהאלדסאלמהש';let drops=[],speeds=[],cols=0,font=18;function resize(){canvas.width=innerWidth;canvas.height=innerHeight;cols=Math.floor(canvas.width/font);drops=Array.from({length:cols},()=>Math.random()*-80);speeds=Array.from({length:cols},()=>.25+Math.random()*.75)}resize();addEventListener('resize',resize);function draw(){ctx.fillStyle='rgba(7,22,38,.055)';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.font=font+'px serif';for(let i=0;i<cols;i++){ctx.fillStyle='rgba(197,160,89,'+(.1+Math.random()*.32)+')';ctx.fillText(letters[Math.floor(Math.random()*letters.length)],i*font,drops[i]*font);drops[i]+=speeds[i];if(drops[i]*font>canvas.height&&Math.random()>.975)drops[i]=0}}setInterval(draw,42)}
const btn=document.querySelector('.menu-btn'),mob=document.querySelector('.mobile');if(btn&&mob){btn.addEventListener('click',()=>mob.classList.toggle('active'));mob.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>mob.classList.remove('active')))}
